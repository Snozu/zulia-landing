<section id="semilla" class="section-pad semilla" aria-labelledby="semilla-title">
  <div class="page-shell semilla-shell">
    <div class="semilla-copy" data-reveal>
      <p class="kicker">Producto ZulIA · Plataforma de prospección agéntica B2B</p>
      <h2 id="semilla-title">Pon tu prospección a trabajar. <em>Todos los días.</em></h2>
      <p class="semilla-lead">Semilla coordina agentes que encuentran, investigan, califican y contactan compradores potenciales hasta entregar conversaciones con contexto a tu equipo.</p>

      <ul class="semilla-principles" aria-label="Alcance inicial de Semilla">
        <li><span>01</span><strong>Prospectos con contexto</strong></li>
        <li><span>02</span><strong>Correo y seguimiento controlado</strong></li>
        <li><span>03</span><strong>El humano aprueba y cierra</strong></li>
      </ul>

      <div class="semilla-actions">
        <a class="button-primary" href="/semilla/">Conocer Semilla</a>
        <span>Prospección B2B · Con cierre humano</span>
      </div>
    </div>

    <div class="growth-console" data-reveal aria-label="Recorrido comercial operado por Semilla">
      <header class="console-head">
        <div><strong>Semilla</strong><span>Campaña piloto</span></div>
        <p><i></i> Supervisado por ZulIA</p>
      </header>

      <ol class="growth-path">
        <li>
          <span class="path-index">01</span>
          <div><small>Encontrar</small><strong>48 cuentas</strong></div>
          <em>ICP</em>
        </li>
        <li>
          <span class="path-index">02</span>
          <div><small>Validar</small><strong>31 encajan</strong></div>
          <em>Fit</em>
        </li>
        <li>
          <span class="path-index">03</span>
          <div><small>Contactar</small><strong>12 aprobados</strong></div>
          <em>Correo</em>
        </li>
        <li>
          <span class="path-index">04</span>
          <div><small>Cultivar</small><strong>4 respuestas</strong></div>
          <em>Interés</em>
        </li>
        <li class="is-handoff">
          <span class="path-index">05</span>
          <div><small>Entregar</small><strong>2 reuniones</strong></div>
          <em>Humano</em>
        </li>
      </ol>

      <div class="reply-card">
        <div class="reply-meta"><span>Respuesta detectada</span><time>10:42</time></div>
        <p>“Me interesa. ¿Qué día podemos platicar?”</p>
        <strong><i></i> Lista para agendar</strong>
      </div>

      <footer class="console-foot"><span>Sembrar</span><i></i><span>Cultivar</span><i></i><span>Entregar</span></footer>
    </div>
  </div>
</section>

<style>
  .semilla { overflow: hidden; background: var(--paper); color: var(--ink); }
  .semilla-shell { display: grid; grid-template-columns: minmax(0, .8fr) minmax(30rem, 1.2fr); align-items: center; gap: clamp(3rem, 8vw, 8rem); }
  .semilla-copy h2 { max-width: 12ch; margin: 0; font-size: clamp(3rem, 5.6vw, 5.9rem); font-weight: 590; letter-spacing: -.065em; line-height: .91; }
  .semilla-copy h2 em { color: var(--blue-dark); font-style: normal; }
  .semilla-lead { max-width: 35rem; margin: 1.7rem 0 0; color: var(--text-muted); font-size: .96rem; line-height: 1.75; }
  .semilla-principles { display: grid; gap: 0; margin: 2.3rem 0 0; padding: 0; list-style: none; }
  .semilla-principles li { display: grid; grid-template-columns: 2.4rem 1fr; align-items: center; gap: .6rem; padding-block: .8rem; border-top: 1px solid var(--line); }
  .semilla-principles li:last-child { border-bottom: 1px solid var(--line); }
  .semilla-principles span { color: var(--blue-dark); font-family: var(--font-mono); font-size: .55rem; }
  .semilla-principles strong { font-size: .72rem; font-weight: 680; }
  .semilla-actions { display: flex; flex-wrap: wrap; align-items: center; gap: 1rem 1.3rem; margin-top: 2rem; }
  .semilla-actions > span { color: var(--text-muted); font-size: .58rem; font-weight: 720; letter-spacing: .06em; text-transform: uppercase; }

  .growth-console { position: relative; padding: 1rem; border: 1px solid var(--night-line); border-radius: 2.8rem; background: var(--night); color: var(--night-text); box-shadow: var(--glass-shadow); }
  .console-head { display: flex; align-items: center; justify-content: space-between; gap: 1rem; padding: .8rem 1rem 1.25rem; }
  .console-head div, .console-head strong, .console-head span { display: block; }
  .console-head strong { font-size: 1rem; letter-spacing: -.04em; }
  .console-head span { margin-top: .15rem; color: var(--night-subtle); font-size: .5rem; text-transform: uppercase; }
  .console-head p { display: flex; align-items: center; gap: .45rem; margin: 0; color: var(--night-muted); font-size: .52rem; font-weight: 650; }
  .console-head p i, .reply-card > strong i { width: .42rem; height: .42rem; border-radius: 50%; background: var(--green); box-shadow: 0 0 0 4px color-mix(in srgb, var(--green) 14%, transparent); }

  .growth-path { position: relative; display: grid; margin: 0; padding: 0; overflow: hidden; border-radius: 1.9rem; background: var(--night-panel); list-style: none; }
  .growth-path::before { position: absolute; top: 2rem; bottom: 2rem; left: 3.68rem; width: 1px; background: linear-gradient(var(--night-line), var(--night-accent), var(--green)); content: ''; }
  .growth-path::after { position: absolute; top: 1.8rem; left: 3.48rem; width: .42rem; height: .42rem; border-radius: 50%; background: var(--night-accent); box-shadow: 0 0 0 5px color-mix(in srgb, var(--night-accent) 13%, transparent); content: ''; animation: seed-route 5s ease-in-out infinite; }
  .growth-path li { position: relative; z-index: 1; display: grid; min-height: 4.8rem; grid-template-columns: 2.4rem 1fr auto; align-items: center; gap: .8rem; margin-left: 2.45rem; padding: .65rem 1.1rem .65rem 0; border-bottom: 1px solid var(--night-line); }
  .growth-path li:last-child { border-bottom: 0; }
  .path-index { display: grid; width: 2.4rem; height: 2.4rem; place-items: center; border: 1px solid var(--night-line); border-radius: 50%; background: var(--night-panel); color: var(--night-subtle); font-family: var(--font-mono); font-size: .49rem; }
  .growth-path div small, .growth-path div strong { display: block; }
  .growth-path div small { color: var(--night-subtle); font-size: .48rem; letter-spacing: .05em; text-transform: uppercase; }
  .growth-path div strong { margin-top: .18rem; font-size: .75rem; }
  .growth-path em { padding: .42rem .6rem; border-radius: 1rem; background: var(--night-glass); color: var(--night-muted); font-size: .48rem; font-style: normal; font-weight: 700; text-transform: uppercase; }
  .growth-path .is-handoff .path-index { border-color: var(--green); color: var(--green); }
  .growth-path .is-handoff em { background: color-mix(in srgb, var(--green) 16%, transparent); color: var(--green); }

  .reply-card { position: absolute; z-index: 3; right: -2.25rem; bottom: 4.5rem; width: min(18rem, 54%); padding: 1rem; border: 1px solid var(--glass-border); border-radius: 1.25rem; background: var(--glass-strong); color: var(--ink); box-shadow: var(--glass-shadow); -webkit-backdrop-filter: blur(24px); backdrop-filter: blur(24px); }
  .reply-meta { display: flex; justify-content: space-between; gap: 1rem; color: var(--text-muted); font-size: .46rem; font-weight: 700; letter-spacing: .04em; text-transform: uppercase; }
  .reply-card p { margin: .8rem 0 1rem; font-size: .72rem; line-height: 1.5; }
  .reply-card > strong { display: flex; align-items: center; gap: .5rem; color: var(--blue-dark); font-size: .53rem; text-transform: uppercase; }
  .console-foot { display: flex; align-items: center; gap: .55rem; padding: 1.05rem 1rem .2rem; color: var(--night-subtle); font-size: .47rem; font-weight: 700; letter-spacing: .05em; text-transform: uppercase; }
  .console-foot i { width: 1.3rem; height: 1px; background: var(--night-line); }

  @keyframes seed-route { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(19.2rem); background: var(--green); } }
  @media (max-width: 980px) { .semilla-shell { grid-template-columns: 1fr; } .semilla-copy { max-width: 48rem; } .growth-console { width: min(100% - 2rem, 42rem); justify-self: center; } }
  @media (max-width: 620px) { .growth-console { width: 100%; padding: .65rem; border-radius: 2rem; } .console-head { align-items: flex-start; } .console-head p { max-width: 8rem; text-align: right; } .growth-path li { margin-left: 1.3rem; grid-template-columns: 2.4rem 1fr; } .growth-path::before { left: 2.55rem; } .growth-path::after { left: 2.35rem; } .growth-path em { display: none; } .reply-card { position: relative; right: auto; bottom: auto; width: calc(100% - 1rem); margin: 1rem auto 0; } .semilla-actions { align-items: stretch; flex-direction: column; } .semilla-actions a { width: 100%; } }
  @media (prefers-reduced-motion: reduce) { .growth-path::after { animation: none; } }
</style>
